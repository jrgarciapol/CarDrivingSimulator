pysdl2>=0.9.16
pysdl2-dll>=2.28.0
numpy>=1.24
# Escena 3D en la GPU (opcional: sin el, el juego usa el render de SDL).
# Solo hay ruedas precompiladas hasta Python 3.13: en 3.14 pip intentaria
# compilarlo y necesitaria el compilador de Visual C++, asi que se omite.
# Con Python 3.13 (o con el compilador instalado): pip install moderngl
moderngl>=5.10; python_version < "3.14"
# Solo para el editor de alineaciones (tools/alignment_editor.py); el
# simulador y las pruebas no lo necesitan:
matplotlib>=3.5
